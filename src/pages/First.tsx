import * as React from 'react';

interface IFirstProps {}

const First: React.FunctionComponent<IFirstProps> = (props) => {
    return (
        <>
            <h1>First Page</h1>
        </>
    );
};

export default First;
